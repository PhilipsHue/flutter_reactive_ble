/// States the level of debuglogging within this library
enum LogLevel {
  /// No debugLogging at all. This is the default level within the library.
  none,

  /// Verbose logging, this can be useful when debugging issues.
  verbose,
}
